Import('env')

_INCLUDES = [Dir('../src').abspath]

_SOURCES = ['PParams.cpp']
_SOURCES = [File('../src/mndlkit/params/' + s).abspath for s in _SOURCES]

env.Append(APP_SOURCES = _SOURCES)
env.Append(CPPPATH = _INCLUDES)

Return('env')
